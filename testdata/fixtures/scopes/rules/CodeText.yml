---
# Inline elements are scopes of their own -- siblings of `text`, so an
# ordinary rule does not also run over them. See #1002.
extends: existence
message: "code: '%s'"
level: warning
scope: code
tokens:
  - deprecated

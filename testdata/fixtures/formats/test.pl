#!/usr/bin/perl
# NOTE: parse the CLI flags before dispatch.
use strict;

my $count = 0;    # XXX: this resets on reload.

sub main {
    # TODO: support a --dry-run flag.
    print "done\n";
}

main();

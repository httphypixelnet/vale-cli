# Update the default database connection pool

Update the default database connection pool size.

1. Update the default database connection pool size.

- Update the default database connection pool size.

> Update the default database connection pool size.

| Col |
| --- |
| Update the default database connection pool size. |
